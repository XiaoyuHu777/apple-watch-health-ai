# Data directories

This directory is reserved for local Apple Health data and generated datasets.

- `raw/`: local source files such as the Apple Health `export.xml`.
- `processed/`: cleaned or aggregated datasets produced by the project.

Both directories are ignored by Git because they may contain sensitive personal
health information. Do not commit or upload their contents.

